import React from 'react'; import Card from '../ui/Card.jsx'; export default function Departments({data}){ const items=(data?.departments?.items)||[]; return <div className='grid gap-4'>{items.map((d,i)=>(<Card key={i} title={d.name}><div>{d.scope}</div></Card>))}</div> }